# Copy this file to .env and set the private address of your Floci VM.
FLOCI_HOST_IP=192.168.1.50
FLOCI_VERSION=1.7.0
FLOCI_REGION=us-east-1

# Docker containers created by Floci use this Compose service name to reach it.
FLOCI_INTERNAL_HOSTNAME=floci
